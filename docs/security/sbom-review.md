# SBOM Review — Project 9

## Purpose

This document records the review of the Software Bill of Materials (SBOM)
generated by the CommunityPulse security workflow.

The review confirms what dependencies and other software components were
present in the CI-generated build inventory and records known security
follow-up items.

## SBOM Identification

| Field | Value |
|---|---|
| SBOM format | CycloneDX |
| Specification version | 1.6 |
| Component count | 82 |
| Generator | Syft v1.42.3 |
| Workflow | `.github/workflows/security.yml` |
| Artifact | `sbom.cdx.json` |
| Review date | 2026-09-16 |

## Key Application Dependencies

The following security-relevant application dependencies were identified
in the reviewed SBOM:

| Component | Version | Review |
|---|---:|---|
| `next` | 14.2.35 | Known dependency concern already tracked in `docs/security/known-risks.md`; requires follow-up through dependency management. |
| `@clerk/nextjs` | 6.39.6 | Present in the build inventory. |
| `@prisma/client` | 5.22.0 | Present in the build inventory. |
| `@upstash/ratelimit` | 2.0.8 | Present in the build inventory and used for application rate limiting. |
| `@upstash/redis` | 1.38.4 | Present in the build inventory and used by the rate-limiting implementation. |

## CI / Supply-Chain Components

The SBOM also identifies GitHub Actions and related CI components, including:

- `actions/checkout`
- `actions/setup-node`
- `actions/setup-python`
- `actions/upload-artifact`
- `gitleaks/gitleaks-action`
- `anchore/sbom-action`
- `bridgecrewio/checkov-action`

Some actions appear at multiple versions in the generated inventory because
the SBOM includes dependency/workflow-related components from the repository
and installed dependency tree.

The CI workflow itself is reviewed separately through the repository's
security workflow and Semgrep findings.

## Known Security Concerns

### Next.js

The reviewed SBOM confirms that the application uses:

```text
next 14.2.35
This is consistent with the known dependency concern already documented in
docs/security/known-risks.md.

The SBOM review therefore confirms that the known dependency is actually
present in the reviewed CI artifact rather than relying only on
package.json or package-lock.json.

The dependency should remain tracked until an appropriate upgrade or other
documented remediation is completed.

npm Audit Findings

The corresponding CI run reported:

1 high vulnerability
1 critical vulnerability

The audit step is currently configured with continue-on-error: true, so
these findings remain visible without preventing the rest of the security
workflow from completing.

The SBOM review does not independently assign severity to vulnerabilities;
the CI dependency audit remains the source for the vulnerability findings.

SBOM Scope Observations

The SBOM contains:

application dependencies
platform/runtime dependencies
GitHub Actions references
repository workflow files
package-lock.json
dependency workflow files discovered under node_modules

This demonstrates that the generated SBOM represents more than a manually
selected list of application packages.

Follow-Up Required
Continue tracking the known Next.js dependency concern in
docs/security/known-risks.md.
Review dependency audit findings during dependency maintenance.
Keep the SBOM generation step in the security workflow.
Complete build provenance/attestation in Phase 2B so that the generated
build artifact can be cryptographically associated with the repository
workflow and source.
Review Decision

SBOM review complete.

The CI-generated SBOM was downloaded and inspected directly. It is a valid
CycloneDX 1.6 SBOM containing 82 components.

The review confirms the expected major application dependencies and records
the known dependency follow-up items. No claim is made that the SBOM itself
constitutes a vulnerability scan; vulnerability findings remain tracked
through the dependency audit process.

Evidence

The reviewed artifact was:

sbom.cdx.json

Local verification commands:

ls -lh sbom.cdx.json
jq '.bomFormat, .specVersion, (.components | length)' sbom.cdx.json
jq '.components[] | {name, version, type}' sbom.cdx.json

Expected identification output:

"CycloneDX"
"1.6"
82

Key dependency verification:

jq -r '.components[] |
  select(
    .name == "next" or
    .name == "@clerk/nextjs" or
    .name == "@prisma/client" or
    .name == "@upstash/ratelimit" or
    .name == "@upstash/redis"
  ) |
  "\(.name) \(.version)"' sbom.cdx.json

Result:

@clerk/nextjs 6.39.6
@prisma/client 5.22.0
@upstash/ratelimit 2.0.8
@upstash/redis 1.38.4
next 14.2.35

